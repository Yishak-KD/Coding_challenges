An object has a property (which clarifies about the object) and we have method (which adds behaviour to the object), we add a behaviour to an object using function

this keyword in JavaScript represents the current object property

let laptop = {
    cpu: 'i9',
    ram: 16,
    brand: 'HP',

    getConfig: function () {
        console.log(this.cpu); // I can also do (laptop.cpu)
    }
}

laptop.getConfig();
